


const creatDb = `CREATE DATABASE school`
const TeacherT = ` CREATE TABLE teachers (
    id int AUTO_INCREMENT PRIMARY KEY,
    first_name varchar(100),
    last_name varchar(100),
    address varchar(200),
    phone_number int
)`

const teachers = [
    []
]